const valores1 = [ 1,3,5 ]
const valores2 = [ 6,7,9 ]

/* { se combinan en un arreglo de arreglo, es decir una matriz} */
console.log( [ ...valores1, ...valores2] )
/* { se crea un solo arreglo con los valores anezados de los dos} */
console.log( [ ...valores1, ...valores2] )